export const DEV_API_URL = "http://localhost:8000";
export const DEFAULT_REQUEST_TIMEOUT_MS = 120000;
export const BATCH_REQUEST_TIMEOUT_MS = 300000;

export const STORAGE_KEYS = {
  productType: "defectDetector.productType",
  threshold: "defectDetector.threshold"
};
