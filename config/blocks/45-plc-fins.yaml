# Omron FINS/UDP — только настройки таймаутов D4400–D4404 (картинка «Настройки»).
# Готовность / ошибка / брак линий — дискретные входы X4–X7 через IoInputMonitor DO
# (см. 52-io-input.yaml reject). CIO 240.15 «Сброс дискретных входов» не используем.
# Сигналы/карта: plk/register-map.yaml

plc_fins:
  enabled: true
  host: "169.254.220.231"
  port: 9600
  dest_node: 231
  src_node: 254
  response_timeout_ms: 1000
  pulse_ms: 200
  queue_size: 64
  register_map_path: "plk/register-map.yaml"
  vision_ready_signal: "vision_ready"
  vision_fault_signal: "vision_fault"
