# PLC register map (Omron MAP 25_247)

Исходные экспорты из CX-Programmer / MAP — файлы `25_247_MAP - *.csv`.

**Рабочая карта для оркестратора:** `register-map.yaml` — её и правьте руками.

## Как поменять сигнал по FINS без правки кода

1. Откройте `plk/register-map.yaml`.
2. Найдите сигнал по `name` или добавьте новый блок в `signals:`.
3. Поля, которые реально едут в FINS:
   - `area` — `CIO` / `W` / `H` / `DM`
   - `address` — `"слово.бит"` (как в таблице: `140.08`, `240.00`)
   - `direction` — `pc_to_plc` (пишем) или `plc_to_pc` (только читаем)
4. Перезапустите оркестратор (карта грузится при старте).

Пример: брак линии 1 писать в Work Area вместо CIO:

```yaml
  - name: reject_line_1
    area: W
    address: "0.06"
    direction: pc_to_plc
    bucket_group_id: 0
```

Служебные имена (не переименовывать): `vision_ready`, `vision_fault`, `reject_line_1`, `reject_line_2`.
На них ссылаются `plc_fins.vision_*_signal` и выбор линии брака по `bucket_group_id`.

## Лист «Состояния / Неисправности / Управление» → FINS

| Колонка | Слова CIO | direction | Что делает оркестратор |
|---------|-----------|-----------|-------------------------|
| Состояния | 140… | `plc_to_pc` (кроме vision/reject) | читает биты |
| Неисправности | 190…191 | `plc_to_pc` (кроме vision_fault) | читает биты |
| Управление | 240… | `pc_to_plc` | пишет биты (сброс аварий и т.п.) |

Техзрение (`vision_ready` / `vision_fault` / `reject_line_*`) в YAML стоят на CIO из этого листа (`140.02`, `190.00`, `140.08/09`) с `pc_to_plc`.

## Два канала на брак / ready / fault

| Канал | Когда | Куда |
|-------|-------|------|
| Дискретные DO | `integration.io_input_monitor_reject.enabled: true` | DO→X4…X7 |
| FINS | тот же флаг `false` (fallback) или ручная панель ПЛК | `area`+`address` из YAML |

Таймауты отбраковки всегда по FINS: **D4400–D4404** (секция `timeouts`).

## Листы CSV

| Файл | Назначение |
|------|------------|
| Память.csv | Области памяти (CIO/D/W/H) |
| Входы.csv | Дискретные сигналы техзрения → ПЛК (W0.xx ← Xn) |
| Состояния-неисправности-управление.csv | CIO 140 / 190 / 240 |
| Настройки.csv | Таймауты отбраковки (DM) — FINS |
